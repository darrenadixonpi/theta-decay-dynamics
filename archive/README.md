# Archive

Version history and early project artifacts tracked in git.

| Path | Contents |
|------|----------|
| `old_pdfs/` | Milestone report PDFs (v4.4 through v6.1) |
| `legacy/` | Pre-v6 source bundle (`Theta_Decay_Report_legacy.7z`) — early Databento scripts and SPY CSV pulls |

**Not in git** (local scratch only):

- `scratch/` — PDF text extraction, one-off rebuild scripts
- `cursor-skills/` — personal Cursor skill copies

Intermediate build PDFs (`*_draft`, `*_build`, `*_layout`, `*_rebuilt`) are kept locally under `old_pdfs/` but not committed.
