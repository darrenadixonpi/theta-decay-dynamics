# Core PDF report build
reportlab>=4.0
pypdf>=4.0
Pillow>=10.0
matplotlib>=3.8
numpy>=1.26
scipy>=1.11

# Optional: Databento validation pipeline (see requirements-databento.txt)
# pip install -r requirements-databento.txt
